CSR template references
=======================

Put additional known-good CSR images in this directory. The application
currently uses config/form_template.json for the machine-readable rules and
these images as human/reference material. Add more reference images as your
sample set grows.

Do not use a completed form as an exact pixel template. Names, dates, numbers,
handwriting, signatures and stamps are expected to vary between valid forms.
